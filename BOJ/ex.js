/*const fs = require('fs');
const input = fs.readFileSync('/dev/stdin').toString().split('\n');
*/
 const input = "2"

for (let i = 1; i < 10; i++) {
    console.log(`${input} * ${i} = ${input*i}`);
}